[project]
name = "reactor-backend"
version = "0.1.0"
description = "Python backend for the reactor simulator"
requires-python = ">=3.9"
dependencies = [
    "fastapi==0.115.12",
    "uvicorn==0.34.2",
]

[tool.uv]
package = false
